## 문제
`TruckCard` 텍스트 오버레이(`top: 62%`)가 너무 아래에 있어 모바일에서 트럭 바퀴와 겹침.

## 수정
`src/components/confesta/TruckCard.tsx` 74행의 `style={{ top: "62%" }}` 값을 **52% ~ 55% 범위로 올림**.

- 라벨/제목/설명/뱃지 전체가 함께 위로 이동
- 바퀴(약 하단 65% 이후 영역)와 텍스트가 겹치지 않도록 여유 확보
- 수정 후 스크린샷으로 모바일/데스크톱 렌더링 확인