## 요약
- 영수증 하단의 "오늘 당신의 AI 교육자 유형" 박스를 좌우 양쪽으로 꽉 차게 넓힘.
- "이미지로 저장" 버튼 아래의 "데모 초기화" 버튼을 삭제.

## 수정 범위
`src/components/confesta/ReceiptCard.tsx`

### 1. AI 교육자 유형 박스 가로 확장
- `PersonaBadge` 컴포넌트 내부의 강조 박스에서 `mx-auto max-w-[260px]` 제거하고 `w-full`로 변경.
- 이미지에서처럼 영수증 내부 좌우 여백까지 박스가 채워지도록 합니다.

### 2. 데모 초기화 버튼 삭제
- `return (...)` 본문의 "이미지로 저장" 버튼 아래 `<button>데모 초기화</button>` 전체를 제거.
- 버튼만 제거하며, 이미지 저장 버튼과 영수증 본문 로직에는 영향을 주지 않습니다.

## 영향
- 영수증 UI에서 유형 카드가 더 넓고 균형 있게 표시됨.
- 데모 데이터 초기화 기능은 더 이상 영수증 탭에서 노출되지 않음(기존 `reset` 함수는 필요시 다른 곳에서 재사용 가능).
- 다른 탭(주문, My콘, 토핑 추가) 및 발표자/관리자 화면에는 변경 없음.