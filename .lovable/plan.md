수령 QR 갱신 주기를 15초에서 300초(5분)로 늘리는 변경입니다.

변경 대상: `src/routes/presenter.tsx`
- `QR_INTERVAL_MS` 상수: `15_000` → `300_000`
- UI 라벨: "15초마다 갱신" → "5분마다 갱신"
- 남은 시간 표시: `Math.ceil((progress / 100) * 15)` → `Math.ceil((progress / 100) * 300)`

영향 범위:
- 발표자 화면의 수령 QR만 갱신 주기가 늘어남
- 주문 QR, 관리자 화면 QR, 청중 스캐너 동작에는 영향 없음
- 서버 측 nonce 검증은 현재 nonce 일치 여부만 확인하므로 별도 수정 불필요